public class Math {

    public static void sum(double a, double b) {
        double c = a + b;
        System.out.printf("%.4f", c); // формат "%.4f" указвает на то, что в результате сложений чисел,
        // будет выводиться число с четыремя знаками после запятой в независимости от того, есть ли дробная часть
    }
}
